# HANDOFF

This file is for a fresh agent instance with no prior conversation context.

## Project State

This repo is a Django + DuckDB + Parquet invoice-control dashboard platform replacing Power BI.

The user’s real goal is no longer “generic scaffolding”; the active work is:

- reproduce Power BI control reporting from SQL Server data
- keep the same business results
- improve the UI/UX relative to Power BI

The sync pipeline is working against a real client (`viave`) and writes RG and VA data into local Parquet datasets.

## Exact Environment

Use the existing `.venv`. Do not create a second env unless the user explicitly asks.

- Virtual environment: `/Users/pedroprevost/ontwikkel/factuurcontrole/.venv`
- Python: `3.13.5`
- Django: `5.2.12`
- duckdb: `1.5.0`
- pandas: `3.0.1`
- pyarrow: `23.0.1`
- pyodbc: `5.3.0`
- python-dotenv: `1.2.2`
- psycopg: `3.3.3`
- Package install mode used here: `.venv` + `requirements.txt` / editable install
- SQL Server ODBC driver confirmed working here: `ODBC Driver 17 for SQL Server`

Important:

- avoid creating Conda/Poetry/Nix duplicates
- use `.venv/bin/python` or activate `.venv`
- current docs assume `python -m venv .venv`

## SQL Server / Source Context

Known source schema:

- SQL Server schema: `facturatie`
- `DataSourceConfig.extra_params` must include:

  ```json
  {"schema": "facturatie"}
  ```

Known current-run source:

- `facturatie.stuurtabel2_last`
- fields used:
  - `id` -> current `stuurtabel_id`
  - `Omschrijving`
  - `soortvervoer`

Run type rule:

- `RG` -> use `gecontroleerdeRittenDetail` and `gecontroleerdeRoutesDetail`
- `VA` -> use `gecontroleerdeVARittenDetail`

Control metadata:

- `facturatie.controle`
- `omschrijving` is the control description

Executed controls:

- built from `controle` + `controleDone`

## Current Data Contracts

Manifest current dataset:

- path: `data/client=<slug>/dataset=manifest/current/*.parquet`
- fields:
  - `client_slug`
  - `stuurtabel_id`
  - `omschrijving`
  - `soortvervoer`
  - `is_current`
  - `sync_timestamp`

RG datasets:

- `ritten_detail`
- `routes_detail`
- `ritten_controls_long`
- `routes_controls_long`

VA datasets:

- `va_ritten_detail`
- `va_ritten_controls_long`

Executed controls dataset:

- `executed_controls`
- currently includes:
  - `stuurtabel_id`
  - `controle_id`
  - `id`
  - `naam`
  - `omschrijving`
  - `soortvervoer`
  - plus sync metadata columns

Long control datasets:

- generated by expanding `resultaat_<id>`, `tekst_<id>`, `controlewaarde_<id>`, `dempelwaarde_<id>`
- fields:
  - `client_slug`
  - `stuurtabel_id`
  - `entity_type`
  - `entity_id`
  - `datum`
  - `vervoerder`
  - `perceel`
  - `controle_id`
  - `resultaat`
  - `tekst`
  - `controlewaarde`
  - `dempelwaarde`
  - `synced_at`
  - `is_deviation`

## Current Dashboard / Reporting Status

Implemented:

- main dashboard with `stuurtabel_id` selector
- selector labels show `stuurtabel_id`, `soortvervoer`, `omschrijving`
- executed controls are links to control routes
- control detail pages use full-width layout
- control detail tables are compact and no-wrap

Implemented control pages:

- Control 1
  - route: `/clients/<slug>/controls/1/`
  - logic in `backend/apps/analytics/services.py`
  - based on `resultaat_1` / `tekst_1`
- Control 8
  - route: `/clients/<slug>/controls/8/`
  - based on `resultaat_8`, `controlewaarde_8`, `dempelwaarde_8`, planned and realized times

All other controls:

- generic route exists
- placeholder page shown until implemented

Important UI decisions already made:

- detail pages should be full-width
- location IDs should not be shown in location columns
- dates on detail pages should be `dd-mm-yyyy`
- times should be `HH:MM`
- empty values should render blank, not `None`
- no extra per-control buttons on the dashboard; use executed-control links instead

## Recent Sync Result

Latest good sync metadata was read via:

```bash
.venv/bin/python backend/manage.py show_sync_metadata --client viave
```

Key results:

- `sync_timestamp`: `2026-03-18T19:06:43.336250+00:00`
- current counts:
  - `ritten_detail`: `669144`
  - `routes_detail`: `134552`
  - `va_ritten_detail`: `217214`
  - `executed_controls`: `852`
  - `ritten_controls_long`: `1700914`
  - `routes_controls_long`: `24242`
  - `va_ritten_controls_long`: `574926`
  - `manifest`: `70`

Manifest validation already confirmed:

- RG and VA runs are present
- examples:
  - `352 / RG / 2026-02 WDK RG`
  - `351 / VA / 2026-02 WDK VA`

## Recent Verification / Logs

Recent commands used successfully:

- `python -m compileall ...` on changed Python files
- `python manage.py sync_client_data --client viave`
- `python manage.py show_sync_metadata --client viave`
- DuckDB schema checks against Parquet datasets in `data/client=viave/...`

Known working user statement:

- user confirmed recent sync succeeded after VA support was added

Known live SQL caveat:

- direct ad hoc SQL Server probes can intermittently fail with login timeout if VPN/network is unstable
- when that happens, prefer inspecting the synced Parquet + `show_sync_metadata` before trying more live DB discovery

## Key Files To Read First

Core docs and handover:

- `/Users/pedroprevost/ontwikkel/factuurcontrole/README.md`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/HANDOFF.md`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/todo.md`

Pipeline:

- `/Users/pedroprevost/ontwikkel/factuurcontrole/sync_pipeline/sqlserver/extractor.py`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/sync_pipeline/jobs/sync_client.py`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/sync_pipeline/parquet/manifest.py`

Analytics and dashboards:

- `/Users/pedroprevost/ontwikkel/factuurcontrole/backend/apps/analytics/services.py`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/backend/apps/dashboards/views.py`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/backend/apps/dashboards/urls.py`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/templates/dashboards/client_dashboard.html`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/templates/dashboards/control_1_report.html`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/static/css/app.css`

Reference inputs from user:

- `/Users/pedroprevost/ontwikkel/factuurcontrole/uploads/Controles Smartfact.docx`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/uploads/control1.png`
- `/Users/pedroprevost/ontwikkel/factuurcontrole/uploads/control8.png`

## Business Meaning Already Captured

The uploaded Word document contains Dutch descriptions for controls 0-24 and 1001-1008.

Examples already used:

- Control 1 = `Bestelling ook in SW?`
- Control 8 = `Overschrijden reistijd`
- VA controls include `1001` to `1008`

Important domain observation:

- RG/VA detail tables are wide tables with one set of result columns per control
- final, trustworthy deviation logic must become control-specific
- current `is_deviation` in long datasets is still heuristic and only a transitional layer

## Open Challenges

1. Control-specific logic is not finalized.
   The current generic `is_deviation` heuristic should eventually be replaced per control.

2. Control report coverage is incomplete.
   Only controls 1 and 8 have implemented detail pages.

3. History behavior is still naive.
   Sync appends history every run even when data is static.

4. Some values still need business validation.
   Example: the exact interpretation of control 8 fields should be compared against Power BI output.

## Next Good Steps

Best next path if continuing feature work:

1. Compare the new control 8 page against Power BI and fix any field or KPI mismatches.
2. Build the next requested control page using the same pattern.
3. Move control page templates to a cleaner generic/shared structure if repetition grows.
4. Implement history deduplication / “previous current becomes history only when current changes”.

Best next path if hardening:

1. Add tests for:
   - RG/VA run selection
   - manifest contract
   - long dataset generation
   - control page query outputs
2. Add structured docs for each implemented control contract.

## Do Not Forget

- The user cares about matching Power BI results, not copying the Power BI look exactly.
- The user explicitly wants a nicer UI than Power BI.
- The user prefers wide, readable detail tables without wrapping.
- Avoid introducing new environment tools or version upgrades unless asked.
